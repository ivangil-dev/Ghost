<div class="modal-content" {{on-key "Enter" (perform this.deleteTagTask)}} data-test-modal="confirm-delete-tag">
    <header class="modal-header">
        <h1>¿Estás seguro de que quieres eliminar esta etiqueta?</h1>
    </header>
    <button type="button" class="close" title="Cerrar" {{on "click" @close}}>{{svg-jar "close"}}<span class="hidden">Cerrar</span></button>

    <div class="modal-body">
        {{#if @data.tag.count.posts}}
            <span class="red">Esta etiqueta está adjunta a <span data-test-text="posts-count">{{gh-pluralize @data.tag.count.posts "artículo"}}</span>.</span>
        {{/if}}
        Estás a punto de eliminar "<strong>{{@data.tag.name}}</strong>". ¡Esto es permanente! Estás avisado, ¿OK?
    </div>

    <div class="modal-footer">
        <button class="gh-btn" type="button" {{on "click" @close}} data-test-button="cancel"><span>Cancelar</span></button>
        <GhTaskButton @buttonText="Borrar" @successText="Borrada" @task={{this.deleteTagTask}} @class="gh-btn gh-btn-red gh-btn-icon" data-test-button="confirm" />
    </div>
</div>